pipeline {
    agent any
    environment {
        DOCKER_IMAGE_NAME = "soniachalouah/angular"
        DOCKER_HUB_CREDENTIALS = 'docker'
        DOCKER_COMPOSE_VERSION = "1.29.2"
    }


    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
            stage('docker Build') {
                    steps {
                        sh 'docker build -t soniachalouah/angular .'
                    }
                }
            stage('docker push') {
                    steps {
                        sh 'docker push soniachalouah/angular:latest'
                    }
                }


            stage('Run Docker Compose') {
                            steps {
                                script {
                                    // Perform Docker login
                                    sh 'docker login -u soniachalouah  -p sonia1234'

                                    // Pull the Docker image
                                    sh 'docker pull soniachalouah/angular'

                                    // Run Docker Compose    s
                                    sh 'docker compose up -d'
                                }
                            }
                        }


            }
        }